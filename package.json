{
  "name": "sahih-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "lint": "solhint 'src/**/*.sol'",
    "lint:ci": "solhint 'src/**/*.sol' --max-warnings 0"
  },
  "devDependencies": {
    "solhint": "^5.0.3"
  }
}
